mutation createEvent(
  $userId: Id!
  $title: String!
  $description: String!
  $start: StartDate!
  $end: EndDate!
) {
  createEvent(
    userId: $userId
    title: $title
    description: $description
    start: $start
    end: $end
  ) {
    id
  }
}
