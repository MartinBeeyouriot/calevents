mutation updateEvent(
  $userId: Id!
  $id: Id!
  $title: String
  $description: String
  $start: StartDate!
  $end: EndDate!
) {
  updateEvent(
    userId: $userId
    id: $id
    title: $title
    description: $description
    start: $start
    end: $end
  ) {
    id
  }
}
